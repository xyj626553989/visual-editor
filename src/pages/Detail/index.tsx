import React, { FunctionComponent } from "react";
import { RouteComponentProps } from "react-router-dom";
const Detail: FunctionComponent<RouteComponentProps> = () => {
  return <div>Detail</div>;
};

export default Detail;
